import Head from 'next/head'; import {useMemo,useState} from 'react'; import ProductGrid from 'components/ProductGrid'; import {products,categories} from 'data';
export default function Products(){const [q,setQ]=useState('');const [cat,setCat]=useState('all');const [sort,setSort]=useState('featured');const list=useMemo(()=>{let a=products.filter(p=>(cat==='all'||p.category===cat)&&(`${p.name} ${p.description} ${p.subtitle}`.toLowerCase().includes(q.toLowerCase())));if(sort==='low')a.sort((x,y)=>(x.price||0)-(y.price||0));if(sort==='high')a.sort((x,y)=>(y.price||0)-(x.price||0));if(sort==='new')a=[...a].reverse();return a},[q,cat,sort]);return <><Head><title>All Products | SHIFAVEDA</title></Head><main><section className="page-hero"><div className="container"><span>SHOP SHIFAVEDA</span><h1>All Products</h1><p>Explore the current SHIFAVEDA catalogue.</p></div></section><section className="section"><div className="container"><div className="summary-card" style={{display:'grid',gridTemplateColumns:'2fr 1fr 1fr',gap:12,marginBottom:24}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products…"/><select value={cat} onChange={e=>setCat(e.target.value)}><option value="all">All categories</option>{categories.map(c=><option key={c.slug} value={c.slug}>{c.title}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">Featured</option><option value="new">Newest</option><option value="low">Price: Low to High</option><option value="high">Price: High to Low</option></select></div>{list.length?<ProductGrid products={list}/>:<div className="empty-box"><h2>No products found</h2><p>Try another search or category.</p></div>}</div></section></main></>}
