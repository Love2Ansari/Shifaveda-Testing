import Head from 'next/head'; import {useState} from 'react'; import {useRouter} from 'next/router';
export default function Track(){const r=useRouter();const [orderNumber,setOrderNumber]=useState('');const [phone,setPhone]=useState('');const [order,setOrder]=useState(null);const [error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');setOrder(null);const res=await fetch('/api/track-order',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({orderNumber,phone})});const d=await res.json();if(!res.ok)setError(d.error);else setOrder(d.order)};return <><Head><title>Track Order | SHIFAVEDA</title></Head><main><section className="section"><div className="container" style={{maxWidth:680}}><div className="summary-card"><h1>Track Order</h1><form className="checkout-form" onSubmit={submit}><label>Order Number<input value={orderNumber} onChange={e=>setOrderNumber(e.target.value)} placeholder="SHV-12345678" required/></label><label>Mobile Number<input value={phone} onChange={e=>setPhone(e.target.value)} inputMode="numeric" maxLength="10" required/></label>{error&&<div className="error-box">{error}</div>}<button className="btn btn-gold large">Track</button></form>{order&&<div style={{marginTop:24}}><h2>{order.order_number}</h2><p>Status: <b>{order.status}</b></p><p>Payment: <b>{order.payment_status}</b></p><p>Total: <b>₹{Number(order.total).toLocaleString('en-IN')}</b></p><p>Delivered to: {order.customer_name}, {order.city}, {order.state}</p></div>}</div></div></section></main></>}
