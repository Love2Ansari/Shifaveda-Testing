import Head from 'next/head'; import {useEffect,useState} from 'react'; import {supabase} from 'lib/supabase'; import {useAuth} from 'context/AuthContext';
export default function Orders(){const {profile}=useAuth();const [rows,setRows]=useState([]);const load=()=>supabase&&supabase.from('orders').select('*').order('created_at',{ascending:false}).then(({data})=>setRows(data||[]));useEffect(()=>{if(profile?.role==='admin')load()},[profile]);if(profile?.role!=='admin')return <main><section className="section"><div className="container">Admin access required.</div></section></main>;const update=async(id,status)=>{await supabase.from('orders').update({status}).eq('id',id);load()};return <><Head><title>Admin Orders | SHIFAVEDA</title></Head><main><section className="section"><div className="container"><h1>Orders</h1>{rows.map(o=><div className="summary-card" style={{marginTop:14}} key={o.id}><div style={{display:'flex',justifyContent:'space-between',gap:20,flexWrap:'wrap'}}><div><b>{o.order_number}</b><p>{o.customer_name} · {o.phone}</p><p>{o.address}, {o.city}, {o.state} - {o.pincode}</p></div><div><b>₹{Number(o.total).toLocaleString('en-IN')}</b><p>{o.payment_method} · {o.payment_status}</p><select value={o.status} onChange={e=>update(o.id,e.target.value)}><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="packed">Packed</option><option value="shipped">Shipped</option><option value="out_for_delivery">Out for delivery</option><option value="delivered">Delivered</option><option value="cancelled">Cancelled</option><option value="refunded">Refunded</option></select></div></div></div>)}</div></section></main></>}
