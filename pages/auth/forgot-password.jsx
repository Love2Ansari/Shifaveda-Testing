import Head from 'next/head'; import Link from 'next/link'; import {useState} from 'react'; import {supabase} from 'lib/supabase';
export default function Forgot(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const [error,setError]=useState('');const submit=async e=>{e.preventDefault();setMsg('');setError('');if(!supabase){setMsg('Demo mode: connect Supabase to enable password reset emails.');return}const {error:x}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/reset-password`});if(x)setError(x.message);else setMsg('Password reset email sent.');};return <><Head><title>Forgot Password | SHIFAVEDA</title></Head><main><section className="section"><div className="container" style={{maxWidth:520}}><div className="summary-card"><h1>Forgot Password</h1><form className="checkout-form" onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>{msg&&<div className="notice">{msg}</div>}{error&&<div className="error-box">{error}</div>}<button className="btn btn-gold large">Send Reset Link</button></form><p style={{marginTop:16}}><Link href="/auth/login">Back to Login</Link></p></div></div></section></main></>}
