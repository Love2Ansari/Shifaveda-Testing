import Head from 'next/head'; import Link from 'next/link'; import {useRouter} from 'next/router';
export default function OrderSuccess(){const r=useRouter();const id=r.query.id;return <><Head><title>Order Confirmed | SHIFAVEDA</title></Head><main><section className="section"><div className="container" style={{maxWidth:720}}><div className="summary-card"><span className="eyebrow">ORDER CONFIRMED</span><h1>Thank you for your order.</h1><p>Your order number is <b>{id||'—'}</b>.</p><p>We have recorded your customer details, delivery address, purchased products and order amount in the order system when the database is connected.</p><div style={{display:'flex',gap:12,flexWrap:'wrap'}}><Link className="btn btn-gold" href={`/track-order?order=${encodeURIComponent(id||'')}`}>Track Order</Link><Link className="btn btn-dark" href="/products">Continue Shopping</Link></div></div></div></section></main></>}
